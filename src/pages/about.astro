---
import BaseLayout from "../layouts/BaseLayout.astro"
import { artworks, categories } from "../lib/artworks"
import { url } from "../lib/url"

const facts = [
  ["作品总数", `${artworks.length} 件`],
  ["展厅数量", `${categories.length} 个`],
  ["原始文件", "全部保留在 wallpaper 仓库"],
  ["图片转码", "CDN 按需输出 WebP"],
  ["前端框架运行时", "0 KB（Astro 静态输出）"],
  ["第三方字体请求", "0（使用系统字体栈）"],
  ["分析 / 追踪脚本", "0"],
]
---

<BaseLayout
  title="关于本站"
  description="奶龙艺术宇宙是一个非官方同人企划的画廊站：开源、静态、无追踪，图片全部引自公开壁纸仓库。"
>
  <section class="section wrap wrap-narrow">
    <p class="eyebrow">ABOUT</p>
    <h1 style="font-size:var(--fs-3xl);margin:var(--s-2) 0 var(--s-5)">关于本站</h1>

    <p style="color:var(--c-text-dim);font-size:var(--fs-lg)">
      奶龙艺术宇宙（Nailong Art Universe）是
      <a href="https://github.com/Nailong-Studio" rel="noopener" style="color:var(--c-accent)">Nailong-Studio</a>
      组织下的作品展示站。它把散落在壁纸仓库里的 {artworks.length} 张奶龙主题作品，整理成五个有叙事关系的展厅。
    </p>

    <h2 style="margin-top:var(--s-8);font-size:var(--fs-xl)">三个原则</h2>
    <ol style="color:var(--c-text-dim);padding-left:1.2em;display:grid;gap:var(--s-3)">
      <li><strong style="color:var(--c-text)">不复制资产。</strong>本站不把原图打进构建产物，而是引用 wallpaper 仓库的公开文件并经 CDN 转码，仓库轻、部署快、单一事实源不分裂。</li>
      <li><strong style="color:var(--c-text)">不打扰观众。</strong>没有自动播放、没有强制转场、没有音效。系统开了「减弱动态效果」我们就完全静止，另有独立的一键专注模式。</li>
      <li><strong style="color:var(--c-text)">不收集数据。</strong>没有 Cookie、没有分析脚本、没有第三方嵌入。站点是纯粹静态文件。</li>
    </ol>

    <h2 style="margin-top:var(--s-8);font-size:var(--fs-xl)">技术栈</h2>
    <ul style="color:var(--c-text-dim);padding-left:1.2em;display:grid;gap:var(--s-2)">
      <li>构建：<code>Astro</code>（静态输出 + Islands 架构，页面默认零 JS）</li>
      <li>内容管线：Node 脚本扫描壁纸清单，<code>sharp</code> 在构建期生成 LQIP 与主色</li>
      <li>图片：缩略图本地 WebP，大图走 CDN 按 <code>srcset</code> 四档宽度实时转码</li>
      <li>交互：原生 <code>&lt;dialog&gt;</code> 灯箱、IntersectionObserver 进场、零框架</li>
      <li>部署：GitHub Actions → GitHub Pages（子路径 <code>/website/</code>）</li>
    </ul>

    <h2 style="margin-top:var(--s-8);font-size:var(--fs-xl)">数据</h2>
    <dl class="meta-list">
      {
        facts.map(([k, v]) => (
          <div>
            <dt>{k}</dt>
            <dd>{v}</dd>
          </div>
        ))
      }
    </dl>

    <p style="margin-top:var(--s-8);color:var(--c-text-mute);font-size:var(--fs-sm)">
      本站为非官方同人企划，与任何商业主体无关。所有作品版权归原作者所有，仅供个人欣赏与收藏。
    </p>

    <div style="display:flex;gap:var(--s-3);flex-wrap:wrap;margin-top:var(--s-6)">
      <a class="btn btn--primary" href={url("/gallery/")}>进入画廊 →</a>
      <a class="btn" href="https://github.com/Nailong-Studio/website" rel="noopener">站点源码 ↗</a>
    </div>
  </section>
</BaseLayout>
